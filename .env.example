# Único paso manual de reconstrucción (§19.3): copiar a .env y rellenar.

AAP_DATA_DIR=./data

# LLM providers (config/models.yaml los referencia). Opcionales: sin
# ellos el Model Router degrada limpiamente al provider `mock` (§17.2).
# AAP_OPENAI_BASE_URL=https://api.example.com/v1
# AAP_OPENAI_API_KEY=
# AAP_OPENAI_MODEL=gpt-4o-mini
